# People

One markdown file per person, named with a lowercase slug (`first-last.md`). Copy [../templates/person.md](../templates/person.md).

Nobody is tracked yet. `/onboard` and `/capture` create these files.
